/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',

    basePath: '/alpha-beta_visualization',

    images: {
        unoptimized: true,
      },
};

export default nextConfig;
