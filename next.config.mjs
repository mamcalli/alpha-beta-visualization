/** @type {import('next').NextConfig} */
const nextConfig = {
    output: 'export',

    basePath: '/alpha-beta-visualization',

    images: {
        unoptimized: true,
      },
};

export default nextConfig;
